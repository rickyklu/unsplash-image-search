/*
get user feedback/interactions
use user search term to make API request
get fetched images and show on screen

components:
app
searchbard imagelist
*/

import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App';

ReactDOM.render(<App />, document.getElementById('root'));
